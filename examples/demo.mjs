import {ContinuityStore} from '../src/core.mjs'; import {FakeProvider,PiAdapter,SideEffectTool} from '../src/adapter.mjs';
const s=new ContinuityStore('./.demo/continuity.db',{mode:'active'});const t=s.createTask('demo','main','implement a feature',{constraints:['preserve compatibility']});const a=new PiAdapter(s,t.task_id,new FakeProvider(['continue']));a.request({messages:[{role:'user',content:'continue'}]});a.runSideEffect('record',new SideEffectTool(x=>({recorded:x})),{opId:`demo-op-${t.task_id}`});const cid=s.handoff(t.task_id,{expectedRevision:1,epoch:1,workspace:{files:['demo']}});console.log(JSON.stringify({status:s.status(t.task_id),checkpoint:s.inspect(cid,t.task_id),resume:s.forkResume(cid,t.task_id,1)},null,2));s.close();
